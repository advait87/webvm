FROM --platform=i386 i386/debian:buster
ARG DEBIAN_FRONTEND=noninteractive
RUN apt-get clean && apt-get update && apt-get -y upgrade
RUN apt-get -y install apt-utils gcc \
	python3 vim unzip ruby nodejs \
	fakeroot dbus base whiptail hexedit \
	patch wamerican ucf manpages \
	file luajit make lua50 dialog curl \
	less cowsay netcat-openbsd
RUN useradd -m morty && echo "morty:password" | chpasswd
COPY --chown=morty:morty ./game /home/morty/
RUN chmod -R +x /home/morty/
COPY ./rick /usr/local/bin/rick
RUN chmod +x /usr/local/bin/rick
# We set WORKDIR, as this gets extracted by Webvm to be used as the cwd. This is optional.
WORKDIR /home/morty/
# We set env, as this gets extracted by Webvm. This is optional.
ENV HOME="/home/morty" TERM="xterm" USER="morty" SHELL="/bin/bash" EDITOR="vim" LETTERTHREE="n"
RUN echo 'root:password' | chpasswd
CMD [ "/bin/bash" ]
